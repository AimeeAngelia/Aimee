package number;

public class TruckBox {

	public static void main(String[] args) {
		// TODO 自动生成的方法存根
		int $length = 4;
		int $wight = 2;
		double length = 1.5;
		double wight = 1.5;
		int x = (int)($length/length);
		int y = (int)($wight/wight);
		System.out.println("The number of boxes that can be placed on the first floor of the cargo area:"+x*y);
	}

}
